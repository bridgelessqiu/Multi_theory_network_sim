import mms.threshold as mt
import mms.utility as mu
import numpy as np

if __name__ == "__main__":
    c = np.array([1,2,3])
    c_bar = np.reciprocal(c.astype(float))
    print(np.ones((2,1)))